<UserControl x:Class="FP300Service.Controls.NumericUpDownControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="22"
             MinWidth="54"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">
    <UserControl.Resources>
        <Style x:Key="SpinnerButtonStyle" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderBrush" Value="#A0A0A0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#FCFCFC" Offset="0" />
                        <GradientStop Color="#E5E5E5" Offset="1" />
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#D8D8D8" Offset="0" />
                                <GradientStop Color="#F2F2F2" Offset="1" />
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.7" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border BorderBrush="#7F9DB9"
            BorderThickness="1"
            Background="White">
        <Grid Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="17" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="PART_TextBox"
                     Grid.Column="0"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="1,0,2,0"
                     VerticalContentAlignment="Center"
                     HorizontalContentAlignment="Right"
                     PreviewTextInput="PartTextBox_OnPreviewTextInput"
                     LostFocus="PartTextBox_OnLostFocus"
                     KeyDown="PartTextBox_OnKeyDown" />

            <Border Grid.Column="1"
                    BorderBrush="#A0A0A0"
                    BorderThickness="1,0,0,0"
                    Background="#ECE9D8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <RepeatButton Grid.Row="0"
                                  Style="{StaticResource SpinnerButtonStyle}"
                                  BorderThickness="0,0,0,1"
                                  FontSize="7"
                                  Content="▲"
                                  Click="IncreaseButton_OnClick" />
                    <RepeatButton Grid.Row="1"
                                  Style="{StaticResource SpinnerButtonStyle}"
                                  FontSize="7"
                                  Content="▼"
                                  Click="DecreaseButton_OnClick" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
